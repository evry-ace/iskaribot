export enum Topic {
    group = 'group',
}

export enum SubscriptionField {
    posts = 'posts',
    comments = 'comments',
    membership = 'membership',
    membership_requests = 'membership_requests',
}